# Project Overview

> **Generated file. Don't hand-edit.** Run `/overview` once you've filled in
> [../project-plan.md](../project-plan.md) and
> [../build-plan.md](../build-plan.md). It replaces
> this file with your project's product context, loaded each session.
> `docs/project-brief.md` stays authoritative for stack and conventions, and each
> spec's `**Status:**` line stays the work queue. When the plans change, re-run `/overview` rather than editing here.

_Not generated yet. Write the two planning docs, then run `/overview`._
